# Train models for xView challenge [link](http://www.xviewdataset.org/):
* Create `TFRecord` for train and test
* Create `label_map.pbtxt`
* Download `model.ckpt` for `faster_rcnn_nas`
* Create `pipeline.config`
